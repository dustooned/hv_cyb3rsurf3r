# v009 Audio-Reactive Terrain Baseline

Preserves the active direct-file runtime after:

- Yellow timing rail target and Space hit/miss boost prototype.
- Optional OGG-first audio analysis with WAV fallback.
- Bass/treble/volume visual reactivity for wave height, shimmer, white grid glow, and terrain-line pulse.
- `assets/audio/test.wav` remains a local ignored file and is not copied into this snapshot.

Next intended change: make audio reactivity affect the whole main grid instead of being visually concentrated by the left-side wave mask.
